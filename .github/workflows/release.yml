name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - uses: dtolnay/rust-toolchain@stable

      - name: Install frontend
        run: npm ci

      - name: Build installer
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: Pulse ${{ github.ref_name }}
          releaseBody: |
            Windows installer (NSIS) and portable zip.

            First SmartScreen warning is expected (unsigned). WebView2 is already on Windows 10/11.
          releaseDraft: false
          prerelease: false
          args: --bundles nsis
          includeUpdaterJson: true
          updaterJsonPreferNsis: true

      - name: Portable zip
        shell: pwsh
        run: |
          $ver = "${{ github.ref_name }}".TrimStart("v")
          $exe = "src-tauri/target/release/pulse.exe"
          if (-not (Test-Path $exe)) { throw "pulse.exe missing" }
          $out = "pulse-$ver-windows-x64.zip"
          Compress-Archive -Path $exe -DestinationPath $out -Force
          gh release upload "${{ github.ref_name }}" $out --clobber
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
